ifconfig / ip a              # IP info
ping google.com              # connectivity
curl -I https://site.com     # test URL headers
wget https://file.tar.gz     # download file
scp file user@host:/path     # copy file over SSH
rsync -avz src/ user@h:/dst/ # sync files efficiently
netstat -tulnp               # list listening ports
ss -ltnp                     # modern replacement for netstat

