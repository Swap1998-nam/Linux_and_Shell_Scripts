ls -lAh        # list files with details, hidden, human-readable sizes
cd /path       # change directory
pwd            # print working directory
mkdir -p dir   # create dir, including parents
rm -rf dir     # remove files/directories forcefully
cp -r src dest # copy directory recursively
mv file dir/   # move/rename
tree           # view directory tree (install separately)
